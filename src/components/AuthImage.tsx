/**
 * AuthImage — stub para TypeScript.
 *
 * Metro resuelve esta importación con la variante de plataforma correcta:
 *   AuthImage.native.tsx  → Android / iOS
 *   AuthImage.web.tsx     → Web
 *
 * TSC no entiende las extensiones de plataforma, así que apuntamos aquí
 * a la variante nativa como referencia de tipos; en tiempo de ejecución
 * Metro usa la variante correcta.
 */
export { AuthImage } from './AuthImage.native';
export type { AuthImageProps } from './AuthImage.native';
